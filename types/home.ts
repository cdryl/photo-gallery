export type PhotoSlot = {
  id: string;
  title: string;
  label: string;
  className?: string;
};

export type Category = {
  title: string;
  slug: string;
  description: string;
  marker: string;
};
